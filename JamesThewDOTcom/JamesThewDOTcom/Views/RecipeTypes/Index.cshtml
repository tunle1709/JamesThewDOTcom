@model IEnumerable<JamesThewDOTcom.Models.RecipeType>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout_Admin.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.RecipeTyName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.RecipeTyName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.RecipeTypeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.RecipeTypeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.RecipeTypeID })
        </td>
    </tr>
}

</table>
