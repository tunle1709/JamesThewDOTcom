@model IEnumerable<JamesThewDOTcom.Models.RegiterContest>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout_Admin.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CookingContest.Title)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CookingContest1.Title)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Customer.UserName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CookingContest.Title)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CookingContest1.Title)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Customer.UserName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.RegisterContestID }) |
            @Html.ActionLink("Details", "Details", new { id=item.RegisterContestID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.RegisterContestID })
        </td>
    </tr>
}

</table>
